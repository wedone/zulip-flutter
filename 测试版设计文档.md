# 引入 MathLive 数学公式输入功能（测试版）

> 在 Zulip Flutter 会话界面的 ComposeBox 中集成 MathLive 数学公式编辑器与虚拟键盘，使用户能够在移动端高效输入数学公式，并一键插入到消息内容中。
>
> **测试版目标**：以最小改动验证可行性，直接使用 `mathlive_studio` 包，不做自定义修改。

## 1. 需求：用户要什么

高中学生在 Zulip 上讨论数学问题时，需要在消息中输入数学公式。当前移动端的系统键盘无法满足以下需求：

| 需求 | 具体描述 |
|------|---------|
| **特殊符号输入** | 积分号 ∫、求和号 ∑、希腊字母 αβγ、集合符号 ∈⊆∪ 等，系统键盘上难以输入 |
| **结构化公式编辑** | 分数 `\frac{}{}`、根号 `\sqrt{}`、上下标 `^{}_`、矩阵等嵌套结构，纯文本输入极其低效 |
| **所见即所得** | LaTeX 源码与渲染结果之间缺乏即时反馈，输入错误难以发现 |

**核心需求**：一个数学专用的虚拟键盘 + 所见即所得的公式编辑器，编辑完成后一键插入到消息内容中。

## 2. 现状：客户原来有什么

### 2.1 已有的数学相关功能（代码验证）

通过实际代码验证，当前 Zulip Flutter 项目具备以下数学相关能力：

| 能力 | 实际位置 | 现状 |
|------|---------|------|
| 消息中的公式渲染 | `lib/widgets/math_widget.dart` | ✅ 已落地。使用 `flutter_math_fork ^0.7.4`，`Math.tex()` 渲染 TeX 源码，支持 `displayMode` 行内/行间，有 `onErrorFallback` |
| KaTeX HTML → TeX 提取 | `lib/model/katex.dart` | ✅ 已落地。`parseMath()` 从服务器返回的 KaTeX HTML 中提取 `<annotation encoding="application/x-tex">` |
| 内容模型中的公式节点 | `lib/model/content.dart` | ✅ 已落地。`MathNode` sealed class，持有 `texSource` 字段，分为 `BlockMathNode` 和 `InlineMathNode` |
| 消息列表中的公式显示 | `lib/widgets/content.dart` | ✅ 已落地。行间用 `SingleChildScrollView` + `MathWidget(displayMode: true)`，行内用 `WidgetSpan` + `MathWidget(displayMode: false)` |
| **公式输入能力** | — | ❌ **不存在**。没有数学键盘、没有公式编辑器 |
| **LaTeX 格式转换** | `lib/model/latex_converter.dart` | ❌ **暂不实现**。测试版跳过发送时的格式转换 |

**关键结论**：项目仅有**公式渲染（只读）能力**，完全没有**公式输入（写）能力**。

### 2.2 已有的 ComposeBox 结构（代码验证）

实际代码中 ComposeBox 的结构如下（`lib/widgets/compose_box.dart`，约 2400 行）：

```
ComposeBox (StatefulWidget)
  └─ _ComposeBoxState
       └─ _ComposeBoxContainer
            └─ _ComposeBoxBody (abstract)
                 ├─ topicInput (仅 _StreamComposeBoxBody)
                 ├─ contentInput (_ContentInput → TextField)
                 └─ 按钮行: [_AttachFileButton] [_AttachMediaButton] [_AttachFromCameraButton]  [←space→]  [_SendButton]
```

**关键观察**：
- `_ContentInput` 是标准的 Flutter `TextField`，由 `ComposeContentController` 管理
- `ComposeContentController.insertionIndex()` 提供光标位置——这是公式插入的接入点
- `ComposeContentController.insertPadded()` 可在光标位置插入文本——这是引用回复等已有功能的插入方式
- 操作按钮行是 `Row`，左对齐为 `composeButtons` 列表，右对齐为 `sendButton`——新按钮加在 `composeButtons` 中即可

### 2.3 可利用的外部资源

| 资源 | 路径 | 说明 |
|------|------|------|
| **mathlive_studio Flutter 包** | `https://pub.dev/packages/mathlive_studio` | 官方 Flutter 封装，提供 `MathLiveEmbeddedEditor` 和 `MathLiveEditorPage`，内置 WebView 桥接、JS Channel 通信、暗色模式支持 |
| **MathLive 库**（CDN） | 由 mathlive_studio 通过 CDN 加载 | mathlive_studio 使用 `https://cdn.jsdelivr.net/npm/mathlive@0.101.2/dist/` 加载 MathLive 核心 JS 和 CSS |

> **测试版策略**：直接使用 `mathlive_studio` 包，不做任何个人修改。所有 MathLive 功能使用默认的 `<math-field>` 和 `mathVirtualKeyboard`。

### 2.4 MathLive 的技术约束

MathLive 是一个 **Web Components 库**（`<math-field>` 自定义元素），**无法直接在 Flutter 原生组件中使用**，必须通过 WebView 桥接。`mathlive_studio` 包已封装好这一桥接逻辑。

## 3. 方案：怎么实现用户需求

### 3.1 引入方式

直接使用 `mathlive_studio` 包（`https://pub.dev/packages/mathlive_studio`），通过 `flutter pub add mathlive_studio` 添加依赖。

`mathlive_studio` 提供的关键组件：
- **`MathLiveEmbeddedEditor`**：嵌入式编辑器 Widget，适合放在 ComposeBox 下方作为面板
- **`MathLiveEditorPage`**：全屏编辑器页面，适合作为独立页面打开

**测试版选择 `MathLiveEmbeddedEditor`**，将其作为面板嵌入 ComposeBox 下方。

> **为什么选择 `MathLiveEmbeddedEditor`**：
> - 面板模式更接近用户指定的 scrollable-keyboard 方案的行为
> - 不离开当前会话页面，交互更流畅
> - `mathlive_studio` 已内置 `<math-field>` + VirtualKeyboard 的完整实现

### 3.2 默认的 mathlive_studio 编辑器特征

`mathlive_studio` 的默认编辑器（`assets/mathlive/mathlive_editor.html`）核心特征：

- **`virtual-keyboard-mode="manual"`**：虚拟键盘不自动弹出，由工具栏按钮控制
- **工具栏**：`[⋯]` 菜单按钮 + `[⌨]` 键盘切换按钮
- **`<math-field>`**：所见即所得的公式编辑区
- **`<math-virtual-keyboard>`**：MathLive 默认虚拟键盘布局（含数字、符号、字母、希腊字母等标签页）
- **暗色模式**：通过 JS `mlMixedSetTheme()` 切换
- **实时同步**：通过 `FlutterLatexSync` JS Channel 将 LaTeX 实时回传 Flutter

```
mathlive_studio 默认布局概览：
┌─────────────────────────────────────────────┐
│ [⋯] 工具栏                          [⌨]     │
├─────────────────────────────────────────────┤
│ <math-field> (公式编辑区，所见即所得)        │
│                                             │
├─────────────────────────────────────────────┤
│ <math-virtual-keyboard> (默认布局)           │
│ ┌─ Layout Toolbar ────────────────────────┐ │
│ │ [numbers] [symbols] [alphabetic] [greek] │ │
│ ├─ 按键行 ────────────────────────────────┤ │
│ │ MathLive 默认的虚拟键盘按键              │ │
│ └──────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 3.3 界面布局方案

#### 原 ComposeBox（代码验证）

```
┌─────────────────────────────────────────────┐
│ _ComposeBoxBody                              │
│ ├─ topicInput (仅 _StreamComposeBoxBody)     │
│ ├─ contentInput (_ContentInput → TextField)  │
│ └─ 按钮行: [📎][📷][📸]    [➤]              │
└─────────────────────────────────────────────┘
```

#### 新增 MathLive 后的布局

```
不变部分省略
├─────────────────────────────────────────────┤
│ _ComposeBoxBody                              │
│ ├─ topicInput (仅 Stream)                    │
│ ├─ contentInput (系统键盘)                   │
│ └─ 按钮行: [📎][📷][📸][⌨️]    [➤]          │
├─────────────────────────────────────────────┤
│ MathLive 编辑面板 (条件显示)                  │
│ └─ MathLiveEmbeddedEditor (mathlive_studio)  │
│     ├─ 工具栏 [⋯] [⌨]                        │
│     ├─ <math-field> 公式编辑区               │
│     └─ <math-virtual-keyboard> 虚拟键盘      │
└─────────────────────────────────────────────┘
```

> **方案选择理由**：`MathLiveEmbeddedEditor` 作为独立面板显示/隐藏，放在 ComposeBox 下方。内容输入框保持原有行为（系统键盘输入文字），数学编辑面板作为独立区域出现，两者互不干扰。

#### 布局状态切换

```
┌─ 系统键盘模式（默认）────────────────────────────────┐
│  ┌─ ComposeBox ────────────────────────────────────┐ │
│  │  contentInput (TextField) ◀── 系统键盘聚焦      │ │
│  │  [📎][📷][📸][⌨️]                    [➤]        │ │
│  └──────────────────────────────────────────────────┘ │
│  ┌─ 系统键盘 ──────────────────────────────────────┐ │
│  └──────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────┘

┌─ 数学键盘模式 ────────────────────────────────────────┐
│  ┌─ ComposeBox ────────────────────────────────────┐ │
│  │  contentInput (TextField) ◀── 可接收公式插入     │ │
│  │  [📎][📷][📸][⌨️✓]                   [➤]        │ │
│  └──────────────────────────────────────────────────┘ │
│  ┌─ MathLive 编辑面板 ─────────────────────────────┐ │
│  │  ┌─ 工具栏 ──────────────────────────────────┐ │ │
│  │  │  [⋯]                            [⌨]       │ │ │
│  │  ├─ <math-field> ────────────────────────────┤ │ │
│  │  │  x = (-b ± √(b²-4ac)) / 2a               │ │ │
│  │  ├─ <math-virtual-keyboard> ─────────────────┤ │ │
│  │  │  [numbers][symbols][alphabetic][greek]    │ │ │
│  │  │  MathLive 默认按键布局                    │ │ │
│  │  └───────────────────────────────────────────┘ │ │
│  └──────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────┘
```

### 3.4 公式插入方案

`mathlive_studio` 的 `MathLiveEmbeddedEditor` 通过 `onLatexChanged` 回调实时返回 LaTeX 内容。测试版采用**点击 [⌨️] 关闭面板时自动插入**的策略：

**插入流程**：

1. 用户在 `<math-field>` 中通过虚拟键盘编辑公式
2. 实时看到渲染后的公式（`onLatexChanged` 回调）
3. 用户再次点击 [⌨️] 关闭数学面板
4. Flutter 层将当前 LaTeX 包装为 `$$...$$`（行内公式）
5. 通过 `ComposeContentController.insertionIndex()` + `value.replaced()` 插入到光标位置
6. 清空内部 LaTeX 状态

**行内 vs 行间**：测试版默认只支持行内公式插入（`$$...$$`）。行间公式支持延后到后续版本。

> **为什么采用关闭时插入而不是 [return] 按键插入**：
> - `mathlive_studio` 默认没有自定义的 commit/return 按键
> - 测试版尽量减少对 `mathlive_studio` 的修改
> - 关闭时插入流程更简单，不需要修改 HTML/JS

### 3.5 数据流

```
用户在 <math-field> 中编辑公式
       ↓
onLatexChanged 回调实时收到 LaTeX
       ↓
_ComposeBoxState 持有 latestLatex 状态
       ↓
用户点击 [⌨️] 关闭面板
       ↓
ComposeContentController.insertionIndex() 获取光标位置
       ↓
controller.value = controller.value.replaced(insertionIndex, '$$' + latex + '$$')
       ↓
面板关闭，用户继续编辑或发送
```

### 3.6 发送时的格式转换

**测试版暂不实现**。发送时保持 `$$...$$` 格式不变。

> **说明**：Zulip 服务器使用 ```` ```math ... ``` ```` 作为公式界定符，但测试版先验证编辑器本身的可行性。格式转换留到后续版本实现。

## 4. 关键代码

### 4.1 添加依赖

在 `pubspec.yaml` 中添加：

```yaml
dependencies:
  mathlive_studio: ^0.1.2
```

### 4.2 ComposeBox 集成

```dart
// lib/widgets/compose_box.dart 中的改动点

// 1. 新增 import
import 'package:mathlive_studio/mathlive_studio.dart';

// 2. 新增状态（在 _ComposeBoxState 中）
bool _mathKeyboardVisible = false;
String _latestLatex = '';

// 3. 切换逻辑
void _toggleMathKeyboard() {
  if (_mathKeyboardVisible) {
    // 关闭时，如果有公式内容则插入
    if (_latestLatex.trim().isNotEmpty) {
      _insertFormula(_latestLatex);
    }
    _latestLatex = '';
  } else {
    FocusScope.of(context).unfocus();
    SystemChannels.textInput.invokeMethod('TextInput.hide');
  }
  setState(() {
    _mathKeyboardVisible = !_mathKeyboardVisible;
  });
}

// 4. 公式插入
void _insertFormula(String latex) {
  final controller = this.controller;
  final i = controller.content.insertionIndex();
  controller.content.value = controller.content.value.replaced(i, '$$$latex$$');
}

// 5. 在 composeButtons 列表中添加按钮（约第 1492 行）
composeButtons.add(IconButton(
  icon: Icon(Icons.keyboard),
  isSelected: _mathKeyboardVisible,
  onPressed: _toggleMathKeyboard,
));

// 6. 在 _ComposeBoxContainer 的 Column children 中条件渲染面板
if (_mathKeyboardVisible)
  MathLiveEmbeddedEditor(
    isDark: isDark,
    onLatexChanged: (latex) {
      _latestLatex = latex;
    },
  ),
```

### 4.3 LaTeX 格式转换

**测试版跳过**（原 4.4 节内容）。

## 5. 需要修改的文件清单

| 文件 | 改动 | 说明 |
|------|------|------|
| `pubspec.yaml` | 添加 `mathlive_studio: ^0.1.2` 依赖 | 直接使用 pub.dev 包 |
| `lib/widgets/compose_box.dart` | 添加 [⌨️] 按钮 + MathLive 面板 + 公式插入逻辑 | 在操作按钮行新增切换按钮，下方条件渲染面板 |

**相比原设计大幅简化**：
- 不需要复制 MathLive 资源到 assets
- 不需要创建 HTML 模板
- 不需要创建 `MathLivePanel` Widget
- 不需要创建 `latex_converter.dart`
- 不需要声明 assets

## 6. 约束与风险

| 约束/风险 | 影响 | 应对策略 |
|-----------|------|---------|
| MathLive 是 Web Components，需 WebView 桥接 | 增加实现复杂度 | `mathlive_studio` 已封装好 WebView 桥接、JS Channel 通信、平台适配 |
| `mathlive_studio` 从 CDN 加载 MathLive | 需要网络连接 | 首次加载需要网络；后续 WebView 可能缓存 |
| WebView 与 Flutter 通信有延迟 | 公式插入延迟 < 50ms | `onLatexChanged` 回调机制延迟可接受 |
| WebView 在 iOS/Android 行为差异 | 需双平台测试 | `mathlive_studio` 已包含平台适配（`_tunePlatform()`） |
| `mathlive_studio` 包约 2MB（含 WebView 依赖） | 增加应用安装包大小 | 这是使用 WebView 方案不可避免的 |
| 系统键盘与虚拟键盘互斥 | 需精确控制显示/隐藏 | 切换时先 `unfocus()` + `TextInput.hide()`，再显示 WebView 面板 |
| `compose_box.dart` 对上游的侵入性 | 需控制改动范围 | 新增代码集中在 `composeButtons` 列表和 `_ComposeBoxContainer` 的 Column，减少合并冲突 |
| **测试版不做格式转换** | 发送的 `$$...$$` 可能不被 Zulip 服务器正确渲染 | 测试版仅验证编辑器可行性，格式转换延后 |

## 7. 交互流程

### 7.1 正常使用流程

```
1. 用户在 ComposeBox 的内容输入框中打字（系统键盘）

2. 用户需要输入数学公式

3. 点击 [⌨️] 按钮（Icons.keyboard）

   → 系统键盘收起

   → MathLive 编辑面板从底部滑入

   → [⌨️] 按钮变为高亮/选中态

4. 用户在 <math-field> 中通过虚拟键盘编辑公式

   → 实时看到渲染后的公式

5. 用户编辑完公式

6. 再次点击 [⌨️] 按钮关闭数学面板

   → 当前公式以 $$...$$ 格式插入到内容输入框的光标位置

   → MathLive 编辑面板滑出

   → [⌨️] 按钮恢复正常态

7. 用户可继续在内容输入框中编辑，或再次打开数学面板编辑更多公式

8. 用户点击 [发送] 发送消息

   → 发送时保持 $$...$$ 格式不变（测试版暂不转换）
```

### 7.2 边界场景

| 场景 | 处理方式 |
|------|---------|
| 数学面板打开时点击内容输入框 | 关闭数学面板，弹出系统键盘 |
| 数学面板打开时点击发送 | 正常发送，数学面板状态不变 |
| 数学面板打开时切换话题 | 保持数学面板状态 |
| 数学面板打开时进入编辑消息模式 | 关闭数学面板，进入编辑模式 |
| 数学面板打开时页面失去焦点 | 数学面板保持但暂停交互 |
| 插入公式时内容输入框为空 | 直接在开头插入 |
| 插入公式时光标在已有文字中间 | 在光标位置插入，前后不加额外空格（`$$` 已自然分隔） |
| 公式内容为空时关闭面板 | 不插入任何内容 |

## 8. 实现路线

### 阶段一：依赖引入

> 目标：让 `mathlive_studio` 在项目中可用

- [ ] 执行 `flutter pub add mathlive_studio` 添加依赖
- [ ] 确认依赖解析成功，项目可正常编译

### 阶段二：ComposeBox 集成

> 目标：在 ComposeBox 中可切换系统键盘/数学键盘，并插入公式

- [ ] 在 ComposeBox 操作按钮行添加 [⌨️] 切换按钮（使用 `Icons.keyboard` 图标）
- [ ] 实现系统键盘与数学面板的互斥切换逻辑（`unfocus()` + `TextInput.hide()` → 显示 WebView 面板）
- [ ] 实现公式插入（关闭面板时 `$$...$$` → `ComposeContentController.value.replaced()`）
- [ ] 点击内容输入框时自动关闭数学面板并弹出系统键盘

### 阶段三：体验优化（可选）

> 目标：打磨交互体验

- [ ] 暗色模式适配（`MathLiveEmbeddedEditor` 的 `isDark` 参数与 Zulip 主题同步）
- [ ] 边界场景完善（编辑消息模式、切换话题、页面失去焦点等）
- [ ] 双平台测试与适配（iOS/Android WebView 差异处理）

## 9. 参考资料

- [mathlive_studio pub.dev 页面](https://pub.dev/packages/mathlive_studio)
- [MathLive 官方文档](https://cortexjs.io/mathlive/)
- [MathLive VirtualKeyboard API](https://cortexjs.io/mathlive/virtual-keyboard/)
- [mathlive_studio 源码](file:///D:/vc/mathlive_editor_package/flutter_mathlive_mixed/)
- [Zulip Markdown 格式文档](https://zulip.com/help/format-your-message-using-markdown)

---

*文档版本：v5.1-beta · 更新时间：2026-07-04 · 测试版*
