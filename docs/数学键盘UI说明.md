# 数学键盘 UI 说明文档

> **用途**：本文档用自然语言描述数学键盘的UI结构和样式，方便非程序员定位需要修改的地方。
>
> **如何使用**：
>
> 1. 找到你想修改的UI元素
> 2. 在"待填写"区域写下你的修改需求
> 3. 将文档喂给AI，AI会根据文档定位代码并修改

***

## 一、整体结构

### 1.1 容器层级

```
─────────────────────────────────────────────────┐
│  最外层容器 (MathKeyboardToolbar)                 │
│  背景色: 浅灰色 #cacfd7                        │
│  文件: math_keyboard_toolbar.dart               │
│                                                 │
│  ────────────────────────────────────────────┐ │
│  │  分类标签栏 (TabBar)  ← 第161-193行         │ │
│  │  底部边框: 白色半透明                        │ │
│  │  文字大小: 14px                             │ │
│  │  选中颜色: 主题图标色                        │ │
│  │  未选中颜色: 主题前景色 50%透明度            │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  ┌────────────────────────────────────────────┐ │
│  │  符号网格区域 (Symbol Grid) ← 第194-213行   │ │
│  │  最大高度: 220px                           │ │
│  │  内边距: 上下4px, 左右2px                   │ │
│  │                                            │ │
│  │  ┌─────┬─────┬─────┬─────┬─────┐           │ │
│  │  │ 按键 │ 按键 │ 按键 │ 按键 │ 按键 │ ...    │ │
│  │  └─────┴─────┴─────┴─────┴─────┘           │ │
│  └────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────
```

### 1.2 整体容器间距

| 属性             | 当前值              | 代码位置                      | 你想改成什么？ |
| -------------- | ---------------- | ------------------------- | ------- |
| 键盘整体内边距(上)     | `0` (无)          | 第165行 Container           |  |
| 键盘整体内边距(下)     | `0` (无)          | 第165行 Container           |  |
| 键盘整体内边距(左)     | `0` (无)          | 第165行 Container           |  |
| 键盘整体内边距(右)     | `0` (无)          | 第165行 Container           |  |
| 键盘整体外边距(上)     | `0` (无)          | 第165行 Container           |  |
| 键盘整体外边距(下)     | `0` (无)          | 第165行 Container           |  |
| 键盘整体外边距(左)     | `0` (无)          | 第165行 Container           |  |
| 键盘整体外边距(右)     | `0` (无)          | 第165行 Container           |  |
| Column子元素间距    | 紧凑(无间距)          | 第168行 Column              |  |
| **标签栏与符号网格间距** | `0` (紧挨，仅底部边框分隔) | 第168-213行 Column children |  |

### 1.3 待填写区域

| 修改项       | 当前值            | 你想改成什么？ |
| --------- | -------------- | ------- |
| 键盘整体背景色   | `#cacfd7` (浅灰) |  |
| 符号网格最大高度  | `220px`        |  |
| 分类标签文字大小  | `14px`         |  |
| 分类标签选中颜色  | 主题图标色          |  |
| 分类标签未选中颜色 | 主题前景色50%透明     |  |

***

## 二、单个按键样式

### 2.1 按键尺寸和样式

**文件位置**: `math_keyboard_toolbar.dart` 第374-527行

```
┌─────────────────────────────┐
│  单个按键 (_SymbolButton)   │
│                             │
│  宽度: 36px                 │
│  高度: 40px                 │
│  外边距(间隔): 1px          │
│  圆角: 6px                  │
│                             │
│  ┌───────────────────────┐  │
│  │  按键内容区           │  │
│  │  背景: 3种状态(见下)   │  │
│  │  边框: 1px 浅灰       │  │
│  │                       │  │
│  │   [符号/文字/LaTeX]   │  │
│  │                       │  │
│  ───────────────────────┘  │
│  ▼ 底部阴影                 │
│  颜色: #8d8f92 (深灰)       │
│  偏移: 向下1px              │
│  模糊: 0 (硬阴影)           │
└─────────────────────────────┘
```

### 2.1.1 按键3种状态颜色

参考 MathLive 虚拟键盘，按键有 **3 种状态**：

| 状态 | 背景色 | 边框色 | 文字色 | 触发时机 |
|------|--------|--------|--------|----------|
| **0. 平常** | `#FFFFFF` (白色) | `#E5E6E9` (浅灰) | `#000000` (黑色) | 默认状态 |
| **1. 按下时** | `#4A6CF7` (蓝色) | `#4A6CF7` (蓝色) | `#FFFFFF` (白色) | 手指按住时 |
| **2. 刚才按过** | `#F0F0F0` (浅灰) | `#D5D6D9` (稍深灰) | `#000000` (黑色) | 松开后保持，直到按其他按键 |

**实现机制**（第36行、第208-209行、第374-527行）：
- `_MathKeyboardToolbarState` 中维护 `_lastPressedKey` 记录上一个被按下的按键
- 按下时更新 `_lastPressedKey`，松开后不再清除
- 每个按键对比自己的 key 与 `_lastPressedKey`，匹配则显示"刚才按过"的浅灰色
- 切换分类标签时，`_SymbolGrid` 使用 `ValueKey(category)` 重建，清除之前的按键状态

### 2.2 按键间距

| 属性        | 当前值     | 代码位置                              | 你想改成什么？ |
| --------- | ------- | --------------------------------- | ------- |
| 按键外边距(四周) | `1px`   | 第383行 `margin: EdgeInsets.all(1)` | _______________ |
| 按键内边距(上)  | `0` (无) | 第380行 Container                   | _______________ |
| 按键内边距(下)  | `0` (无) | 第380行 Container                   | _______________ |
| 按键内边距(左)  | `0` (无) | 第380行 Container                   | _______________ |
| 按键内边距(右)  | `0` (无) | 第380行 Container                   | _______________ |

### 2.3 待填写区域

| 修改项       | 当前值            | 你想改成什么？ |
| --------- | -------------- | ------- |
| 按键宽度      | `36px`         | _______________ |
| 按键高度      | `40px`         | _______________ |
| 按键外边距(四周) | `1px`          | _______________ |
| 按键圆角      | `6px`          | _______________ |
| 按键边框宽度    | `1px`          | _______________ |
| 底部阴影偏移    | `向下1px`        | _______________ |
| 底部阴影模糊    | `0` (硬边)       | _______________ |
| 状态0背景色    | `#FFFFFF` (白色) | _______________ |
| 状态1背景色    | `#4A6CF7` (蓝色) | _______________ |
| 状态2背景色    | `#F0F0F0` (浅灰) | _______________ |
| 状态0边框色    | `#E5E6E9` (浅灰) | _______________ |
| 状态1边框色    | `#4A6CF7` (蓝色) | _______________ |
| 状态2边框色    | `#D5D6D9` (稍深灰)| _______________ |

***

## 三、按键内容显示

### 3.1 内容类型

按键内容分为三种类型：

#### 类型A: Unicode符号 (直接显示文字)

**文件位置**: `math_keyboard_toolbar.dart` 第433-444行

- 显示方式：直接显示文字
- 文字颜色：黑色 `#000000`
- 文字大小根据显示内容长度自动调整：

| 显示内容长度                  | 文字大小   |
| ----------------------- | ------ |
| 1个字符 (如 `α`, `+`, `0`)  | `22px` |
| 2个字符 (如 `αβ`)           | `18px` |
| 3-4个字符 (如 `sin`, `cos`) | `14px` |
| 5个字符以上                  | `12px` |

**待填写**：

| 修改项        | 当前值            | 你想改成什么？ |
| ---------- | -------------- | ------- |
| 文字颜色       | `#000000` (黑色) |  |
| 1字符时文字大小   | `22px`         |  |
| 2字符时文字大小   | `18px`         |  |
| 3-4字符时文字大小 | `14px`         |  |
| 5字符以上文字大小  | `12px`         |  |

***

#### 类型B: LaTeX模板 (渲染数学公式)

**文件位置**: `math_keyboard_toolbar.dart` 第446-466行

- 显示方式：实时渲染LaTeX公式
- 占位符 `#@` 显示为 **蓝色实心方块 ▣** `#0066CC`（已有输入 / 光标位置）
- 占位符 `#?` 显示为 **灰色空心方框 □** `#A0A0A0`（待填空白 / 新输入）
- 渲染失败时回退到文本显示（12px黑色）
- LaTeX渲染时的文字大小：`14px`
- LaTeX渲染时的文字颜色：黑色 `#000000`

**待填写**：

| 修改项       | 当前值            | 你想改成什么？ |
| --------- | -------------- | ------- |
| 占位符 `#@` 颜色（蓝色实心） | `#0066CC` |  |
| 占位符 `#?` 颜色（灰色空心） | `#A0A0A0` |  |
| LaTeX文字大小 | `14px`         |  |
| LaTeX文字颜色 | `#000000` (黑色) |  |
| 渲染失败时文字大小 | `12px`         |  |

***

## 四、分类标签栏

### 4.1 标签结构

**文件位置**: `math_keyboard_toolbar.dart` 第161-193行

```
┌─────────────────────────────────────────────────┐
│  分类标签栏 Container (第172-178行)             │
│  ┌───────────────────────────────────────────┐  │
│  │  TabBar (第179-193行)                     │  │
│  │  常用 | 关系 | 函数 | 希腊字母 | 模板 | 最近 │  │
│  │  ───────────────────────────────────────  │ ← 底部边框
│  └───────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

### 4.2 标签栏容器间距

| 属性       | 当前值        | 代码位置            | 你想改成什么？ |
| -------- | ---------- | --------------- | ------- |
| 容器内边距(上) | `0` (无)    | 第172行 Container |  |
| 容器内边距(下) | `0` (无)    | 第172行 Container |  |
| 容器内边距(左) | `0` (无)    | 第172行 Container |  |
| 容器内边距(右) | `0` (无)    | 第172行 Container |  |
| 容器外边距(上) | `0` (无)    | 第172行 Container |  |
| 容器外边距(下) | `0` (无)    | 第172行 Container |  |
| 标签栏高度    | 40px                | SizedBox(height: 40)包裹TabBar |  |

> **注意**：分类标签栏保持**左对齐**，不居中。只有下方的按键区域整体居中。

### 4.3 标签栏底部边框

| 属性   | 当前值        | 代码位置                                        | 你想改成什么？ |
| ---- | ---------- | ------------------------------------------- | ------- |
| 边框位置 | 底部         | 第174行 `Border(bottom: ...)`                 |  |
| 边框颜色 | 白色 60%透明度  | 第175行 `Colors.white.withValues(alpha: 0.6)` |  |
| 边框宽度 | `1px` (默认) | 第174行 BorderSide                            |  |

### 4.4 TabBar 标签样式

| 属性         | 当前值         | 代码位置                                     | 你想改成什么？ |
| ---------- | ----------- | ---------------------------------------- | ------- |
| 标签是否可滚动    | 是           | 第180行 `isScrollable: true`               |  |
| 标签对齐方式     | 左对齐(start)  | 第181行 `tabAlignment: TabAlignment.start` |  |
| 选中文字颜色     | 主题图标色       | 第182行 `labelColor: designVariables.icon` |  |
| 未选中文字颜色    | 主题前景色 50%透明 | 第183行 `unselectedLabelColor`             |  |
| 指示器(下划线)颜色 | 主题图标色       | 第184行 `indicatorColor`                   |  |
| 指示器大小      | 与标签文字同宽     | 第185行 `TabBarIndicatorSize.label`        |  |
| 选中文字大小     | `14px`      | 第186行 `labelStyle`                       |  |
| 选中文字粗细     | 中等(w500)    | 第186行 `fontWeight: FontWeight.w500`      |  |
| 未选中文字大小    | `14px`      | 第187行 `unselectedLabelStyle`             |  |
| 未选中文字粗细    | 默认(400)     | 第187行 `unselectedLabelStyle`             |  |

### 4.5 TabBar 内部间距

| 属性            | 当前值             | 代码位置                  | 你想改成什么？ |
| ------------- | --------------- | --------------------- | ------- |
| 标签之间间距        | TabBar默认(约16px) | 第179行 TabBar          |  |
| 标签内边距(上)      | TabBar默认(约12px) | 第179行 TabBar          |  |
| 标签内边距(下)      | TabBar默认(约12px) | 第179行 TabBar          |  |
| 标签内边距(左)      | TabBar默认(约12px) | 第179行 TabBar          |  |
| 标签内边距(右)      | TabBar默认(约12px) | 第179行 TabBar          |  |
| 指示器(下划线)与文字间距 | `0` (紧贴)        | 第185行 `indicatorSize` |  |

***

## 五、符号网格布局

### 5.1 "常用"分类特殊布局

**文件位置**: `math_keyboard_toolbar.dart` 第258-300行

"常用"分类分为**左右两栏**，整体居中：

```
──────────────────────────────────────────────┐
│  SingleChildScrollView (第259行)            │
│  padding: 上下4px, 左右2px                    │
│  ┌────────────────────────────────────────┐  │
│  │  Center (第261行) ← 按键整体居中       │  │
│  │  ┌──────────────────────────────────┐  │  │
│  │  │  Row (第262行)                   │  │  │
│  │  │  mainAxisSize: min (紧凑)        │  │  │
│  │  │  ┌────────┐ ┌────────┐          │  │  │
│  │  │  │ 左栏   │ │ 右栏   │          │  │  │
│  │  │  │ Wrap   │ │ Wrap   │          │  │  │
│  │  │  │ 0 1 2  │ │ A B C  │          │  │  │
│  │  │  │ 3 4 5  │ │ D E F  │          │  │  │
│  │  │  │ ...    │ │ ...    │          │  │  │
│  │  │  └──────── └────────┘          │  │  │
│  │  └──────────────────────────────────┘  │  │
│  ────────────────────────────────────────┘  │
──────────────────────────────────────────────┘
```

### 5.1.1 常用分类网格间距

| 属性       | 当前值                | 代码位置                                               | 你想改成什么？ |
| -------- | ------------------ | -------------------------------------------------- | ------- |
| 网格外边距(上) | `0` (无)            | 第259行 SingleChildScrollView                        |  |
| 网格外边距(下) | `0` (无)            | 第259行 SingleChildScrollView                        |  |
| 网格内边距(上) | `4px`              | 第260行 `padding: EdgeInsets.symmetric(vertical: 4, horizontal: 2)` |  |
| 网格内边距(下) | `4px`              | 第260行 `padding: EdgeInsets.symmetric(vertical: 4, horizontal: 2)` |  |
| 网格内边距(左) | `2px`              | 第260行 `padding: EdgeInsets.symmetric(vertical: 4, horizontal: 2)` |  |
| 网格内边距(右) | `2px`              | 第260行 `padding: EdgeInsets.symmetric(vertical: 4, horizontal: 2)` |  |
| **居中对齐** | **是**              | **第261行 Center**                                   |  |
| Row宽度    | 紧凑(自适应)            | 第263行 `mainAxisSize: MainAxisSize.min`             |  |
| 左右栏之间间距  | `4px` (SizedBox)     | 第280行 `const SizedBox(width: 4)`                   |  |
| 行与行之间间距  | `2px` (按键margin叠加) | 第267行 Wrap                                         |  |
| 列与列之间间距  | `2px` (按键margin叠加) | 第267行 Wrap                                         |  |
| Wrap对齐方式 | 垂直居中               | 第268行 `runAlignment: WrapAlignment.center`         |  |
| 左栏对齐方式  | 左对齐(默认)            | 第267行 Wrap                                         |  |
| 右栏对齐方式  | 右对齐                | 第283行 `alignment: WrapAlignment.end`               |  |

### 5.1.2 待填写区域

| 修改项       | 当前值       | 你想改成什么？ |
| --------- | --------- | ------- |
| 左右栏布局     | 是         |  |
| 左栏内容      | 数字+运算符+括号 |  |
| 右栏内容      | 字母+希腊字母   |  |
| 网格内边距(上下) | `4px`     |  |
| 网格内边距(左右) | `2px`     |  |
| 左右栏之间间距  | `4px`     |  |

***

### 5.2 其他分类布局

**文件位置**: `math_keyboard_toolbar.dart` 第338-354行

其他分类（关系、函数、希腊字母、模板）使用**单栏滚动布局，整体居中**：

```
┌──────────────────────────────────────────────┐
│  SingleChildScrollView (第339行)            │
│  padding: 上下4px, 左右2px                    │
│  ┌────────────────────────────────────────┐  │
│  │  Center (第341行) ← 按键整体居中       │  │
│  │  ┌──────────────────────────────────┐  │  │
│  │  │  Wrap (第342行)                  │  │  │
│  │  │  [符号1] [符号2] [符号3] ...     │  │  │
│  │  │  [符号5] [符号6] [符号7] ...     │  │  │
│  │  └──────────────────────────────────┘  │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### 5.2.1 其他分类网格间距

| 属性       | 当前值                | 代码位置                                               | 你想改成什么？ |
| -------- | ------------------ | -------------------------------------------------- | ------- |
| 网格外边距(上) | `0` (无)            | 第339行 SingleChildScrollView                        |  |
| 网格外边距(下) | `0` (无)            | 第339行 SingleChildScrollView                        |  |
| 网格内边距(上) | `4px`              | 第340行 `padding: EdgeInsets.symmetric(vertical: 4, horizontal: 2)` |  |
| 网格内边距(下) | `4px`              | 第340行 `padding: EdgeInsets.symmetric(vertical: 4, horizontal: 2)` |  |
| 网格内边距(左) | `2px`              | 第340行 `padding: EdgeInsets.symmetric(vertical: 4, horizontal: 2)` |  |
| 网格内边距(右) | `2px`              | 第340行 `padding: EdgeInsets.symmetric(vertical: 4, horizontal: 2)` |  |
| **居中对齐** | **是**              | **第341行 Center**                                   |  |
| 行与行之间间距  | `2px` (按键margin叠加) | 第342行 Wrap                                         |  |
| 列与列之间间距  | `2px` (按键margin叠加) | 第342行 Wrap                                         |  |

### 5.2.2 "最近使用"分类间距

**文件位置**: `math_keyboard_toolbar.dart` 第304-336行

| 属性         | 当前值         | 代码位置                                                          | 你想改成什么？ |
| ---------- | ----------- | ------------------------------------------------------------- | ------- |
| 空提示内边距(四周) | `16px`      | 第309行 `padding: EdgeInsets.all(16)`                           |  |
| 空提示文字大小    | `14px`      | 第312行 `fontSize: 14`                                          |  |
| 空提示文字颜色    | 主题前景色 30%透明 | 第313行 `color: designVariables.foreground.withFadedAlpha(0.3)` |  |
| **居中对齐**   | **是**       | **第307行 Center**                                              |  |
| 符号网格内边距(上) | `4px`       | 第321行 `padding: EdgeInsets.symmetric(vertical: 4)`            |  |
| 符号网格内边距(下) | `4px`       | 第321行 `padding: EdgeInsets.symmetric(vertical: 4)`            |  |

***

## 六、长按变体功能

### 6.1 功能说明

**文件位置**: `math_keyboard_toolbar.dart` 第365-385行

长按某个按键时，会弹出变体选择面板，松开时若未选择则自动插入第一个变体。面板在按键上方弹出，最多 5 列。

### 6.2 两种映射机制

#### 机制A：大写字母 → 小写字母（自动）

**代码位置**: `math_keyboard_toolbar.dart` 第365-372行 `_lowercaseVariant()`

```dart
static List<String>? _lowercaseVariant(String display) {
  if (display.length == 1 && display.codeUnitAt(0) >= 0x41 && display.codeUnitAt(0) <= 0x5A) {
    return [display.toLowerCase()];
  }
  return null;
}
```

- **自动生效**：26个大写字母（A-Z）自动支持长按弹出对应小写字母
- **规则**：单字符且 ASCII 码在 `0x41`~`0x5A` 范围 → 返回小写
- **调用位置**：右栏（字母按键）统一应用此机制

#### 机制B：左栏符号手动映射

**代码位置**: `math_keyboard_toolbar.dart` 第407-419行 `_leftColumnVariant()`

```dart
static List<VariantItem>? _leftColumnVariant(String display) {
  return switch (display) {
    '.' => [TextVariant('⋅'), TextVariant('…')],        // . 长按弹出 ⋅、…
    ',' => [TextVariant(':'), TextVariant(';')],        // , 长按弹出 :、；
    '=' => [TextVariant('≠'), TextVariant('≈'), TextVariant('≅')],  // = 长按弹出 ≠、≈、≅
    '/' => [TextVariant(r'\')],                          // / 长按弹出 \
    '#@^{2}' => [TextVariant('³')],                      // 平方模板长按弹出 ³
    _ => null,                                           // 其他符号无长按
  };
}
```

- **key** = 该按键的 `item.display` 值（与 `math_keyboard_data.dart` 中定义一致）
- **value** = 长按弹出的变体列表（`List<VariantItem>`），多个变体会显示在面板中
- **返回 `null`** = 该按键不支持长按
- **调用位置**：左栏（数字/运算符/模板）统一应用此机制

#### 变体类型

变体支持两种类型（定义在 `math_keyboard_toolbar.dart` 第18-44行）：

| 类型 | 构造 | 显示效果 | 插入内容 |
|------|------|---------|---------|
| `TextVariant` | `TextVariant('⋅')` | 纯文本显示 | 与显示相同 |
| `LatexVariant` | `LatexVariant(latex: r'\frac{#@}{#?}', insert: r'\frac{}{}')` | LaTeX 渲染（含方块占位符） | `insert` 字段，默认等于 `latex` |

**示例**——给 `/` 按键增加 LaTeX 变体：

```dart
'/' => [
  TextVariant(r'\'),               // 纯文本：反斜杠
  LatexVariant(                     // LaTeX 渲染：分数模板预览
    latex: r'\frac{#@}{#?}',
    insert: r'\frac{}{}',
  ),
],
```

面板中 `\` 显示为文字，`\frac{#@}{#?}` 显示为渲染后的分数公式（蓝色方块分子 + 灰色方块分母）。

### 6.3 如何增加/修改映射

#### 新增一个按键的长按变体

1. 确认该按键在数据文件中的 `display` 值
2. 在 `_leftColumnVariant` 的 `switch` 中新增一行：

```dart
'display值' => [TextVariant('变体1'), TextVariant('变体2')],
```

例如给 `√` 按键增加长按变体：
```dart
// math_keyboard_data.dart 中 √ 的 display 是 '√'
'√' => [TextVariant('∛'), TextVariant('∜')],
```

#### 给已有的按键增加更多变体

直接追加到列表中：

```dart
// 原来：'.' => [TextVariant('⋅')]
// 改为：
'.' => [TextVariant('⋅'), TextVariant('…'), TextVariant('•')],
```

#### 增加 LaTeX 变体（带方块占位符预览）

使用 `LatexVariant` 替代 `TextVariant`：

```dart
'/' => [
  TextVariant(r'\'),
  LatexVariant(
    latex: r'\frac{#@}{#?}',    // 面板中渲染为分数公式预览
    insert: r'\frac{}{}',       // 实际插入的文本
  ),
],
```

- `latex` 字段：面板中渲染的 LaTeX 表达式，支持 `#@`（蓝色）和 `#?`（灰色）占位符
- `insert` 字段：实际插入文本编辑器的内容，省略时默认等于 `latex`

#### 注意

- 变体插入的是**文本字符串**（`String`），不是 `MathKeyboardItem`
- `_lowercaseVariant`（大写→小写）和 `_leftColumnVariant`（左栏映射）是**互斥的**，一个按键只会命中其中一种

### 6.4 调用关系

```
_SymbolGrid 构建左栏按键时
  └── onLongPressVariants: _leftColumnVariant(symbol.display)
       └── 根据 display 匹配 → 返回变体列表或 null

_SymbolGrid 构建右栏按键时
  └── onLongPressVariants: _lowercaseVariant(symbol.display)
       └── 判断是否大写字母 → 返回小写或 null
```

***

## 七、符号数据定义

### 7.1 数据文件

**文件位置**: `math_keyboard_data.dart`

所有符号的定义都在这个文件中，包括：

- 符号显示内容 (`display`)
- 实际插入内容 (`output`)
- 所属分类 (`category`)

### 7.2 符号类型说明

| 类型              | 说明                  | 示例                      |
| --------------- | ------------------- | ----------------------- |
| `UnicodeSymbol` | 直接显示和插入Unicode字符    | `α`, `+`, `0`           |
| `LatexSnippet`  | 插入LaTeX代码，光标定位在指定位置 | `\frac{}{}` 光标在第一个`{}`内 |
| `LatexWrapper`  | 包裹选中文本的前后缀          | `$$...$$` 包裹公式          |

### 7.3 占位符约定（与 MathLive 一致）

符号数据定义中（`display` 字段），LaTeX模板使用两种占位符标识按键各部分的用途：

| 占位符 | 含义 | 渲染效果 | 示例 |
|--------|------|---------|------|
| `#@` | 已有输入 / 光标位置 | 蓝色实心方块 ▣ `#0066CC` | `\frac{#@}{#?}` 表示光标在分子 |
| `#?` | 待填空白 / 新输入 | 灰色空心方框 □ `#A0A0A0` | `x_{#?}^{#@}` 表示上标已填，下标待填 |

**规则**：
- `#@` = 用户当前选中的内容 / 光标所在位置（蓝色实心）
- `#?` = 需要用户填入的新内容（灰色空心）
- 两者仅用于 `LatexSnippet` 和 `LatexWrapper` 的 `display` 字段
- `UnicodeSymbol` 的 `display` 是纯文本，不使用占位符

### 7.4 Dart 原始字符串（`r` 前缀）说明

**文件位置**: `math_keyboard_data.dart`

符号定义中，`display` 和 `output` 字段的字符串有时带 `r` 前缀（如 `r'\frac{#@}{#?}'`）：

| 写法 | 含义 | 适用场景 | 示例 |
|------|------|---------|------|
| 带 `r` | **原始字符串**，`\` 不当作转义符 | 包含 `\` 的 LaTeX 表达式 | `r'\frac{#@}{#?}'`、`r'\sqrt{#@}'` |
| 不带 `r` | **普通字符串**，`\` 会被转义 | 纯文本，不含 `\` | `'²'`、`'<'`、`'α'` |

**简单规则**：只要 `display` 或 `output` 里有 `\`（所有 LaTeX 表达式都带），就必须加 `r` 前缀。纯 Unicode 字符（如 `+`、`α`、`²`）不需要。

### 7.5 待填写区域

| 修改项    | 你想添加/删除/修改什么符号？ |
| ------ | --------------- |
| 新增符号   |  |
| 删除符号   |  |
| 修改符号   |  |

***

## 八、给 AI 的使用说明

### 8.1 如何阅读本文档

1. **找到修改目标**：用户会在"待填写"区域填写修改需求
2. **定位代码**：根据文档中的"代码位置"列找到对应的代码行
3. **理解结构**：参考文档中的 ASCII 图示理解容器嵌套关系
4. **执行修改**：根据用户的需求修改代码

### 8.2 代码与 UI 对应关系速查

| UI 元素 | 文件 | 关键行号 |
| ------- | ---- | -------- |
| 键盘整体容器 | `math_keyboard_toolbar.dart` | 第165行 Container |
| 分类标签栏 | `math_keyboard_toolbar.dart` | 第172-193行 |
| 常用分类网格 | `math_keyboard_toolbar.dart` | 第258-300行 |
| 其他分类网格 | `math_keyboard_toolbar.dart` | 第338-354行 |
| 最近使用分类 | `math_keyboard_toolbar.dart` | 第304-336行 |
| 单个按键样式 | `math_keyboard_toolbar.dart` | 第374-527行 |
| 符号数据定义 | `math_keyboard_data.dart` | 全部 |

### 8.3 常见修改示例

**示例1：修改按键颜色**
- 找到第387-397行的 `BoxDecoration`
- 修改 `color`、`border`、`boxShadow` 属性

**示例2：修改按键尺寸**
- 找到第380-382行的 `Container`
- 修改 `width`、`height`、`margin` 属性

**示例3：修改文字大小**
- 找到第433-444行的 `_fontSizeForDisplay` 方法
- 修改不同长度对应的字号

**示例4：修改标签栏样式**
- 找到第179-187行的 `TabBar`
- 修改 `labelStyle`、`unselectedLabelStyle`、`indicatorColor` 等属性

**示例5：修改网格间距**
- 常用分类：第260行 `padding`、第280行 `SizedBox`
- 其他分类：第340行 `padding`

***

## 九、间距速查表

### 9.1 整体间距

| 项目 | 当前值 | 代码位置 |
| ---- | ------ | -------- |
| 键盘整体内边距 | `0` (无) | 第165行 Container |
| 键盘整体外边距 | `0` (无) | 第165行 Container |
| 标签栏与符号网格间距 | `0` (紧挨) | 第168-213行 Column children |

### 9.2 标签栏间距

| 项目 | 当前值 | 代码位置 |
| ---- | ------ | -------- |
| 标签栏容器内边距 | `0` (无) | 第172行 Container |
| 标签栏底部边框 | 白色 60%透明度, 1px | 第174-175行 |
| 标签之间间距 | TabBar默认(约16px) | 第179行 TabBar |

### 9.3 按键间距

| 项目 | 当前值 | 代码位置 |
| ---- | ------ | -------- |
| 按键外边距(四周) | `1px` | 第383行 `margin: EdgeInsets.all(1)` |
| 按键内边距 | `0` (无) | 第380行 Container |

### 9.4 网格间距

| 项目 | 常用分类 | 其他分类 | 最近使用 |
| ---- | -------- | -------- | -------- |
| 网格内边距(上下) | `4px` (第260行) | `4px` (第340行) | `4px` (第321行) |
| 网格内边距(左右) | `2px` (第260行) | `2px` (第340行) | `0` (无) |
| 左右栏之间间距 | `4px` (第280行) | 不适用 | 不适用 |
| 空提示内边距 | 不适用 | 不适用 | `16px` (第309行) |

### 9.5 按键3种状态颜色速查

| 状态 | 背景色 | 边框色 | 文字色 |
| ---- | ------ | ------ | ------ |
| 0. 平常 | `#FFFFFF` (白色) | `#E5E6E9` (浅灰) | `#000000` (黑色) |
| 1. 按下时 | `#4A6CF7` (蓝色) | `#4A6CF7` (蓝色) | `#FFFFFF` (白色) |
| 2. 刚才按过 | `#F0F0F0` (浅灰) | `#D5D6D9` (稍深灰) | `#000000` (黑色) |
