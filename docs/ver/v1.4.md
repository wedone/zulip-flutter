### 数学符号工具栏
- 重构"常用"标签布局，混合排列数字、运算符、括号、上下标
- 成对符号（`$$` `||` `()` `[]` `{}`）输入后光标自动居中
- 新增上标快捷符号 `²` `³` 和 `\vec{}` 向量模板
- 输入框底部新增删除和光标移动按钮
- 长按删除：清空全部文本；长按光标按钮：跳到最前/最后
- 大写字母长按直接输入对应小写字母

### LaTeX 实时预览
- 光标位于公式界定符内时自动渲染预览
- 支持 `$...$` `$$...$$` `\(...\)` `\[...\]`
- 300ms 防抖，输入框失焦后自动隐藏

### 工程优化
- 优化 APK 构建 workflow，简化产物命名
- 统一版本号格式为 `30.0.272-math-vX.Y+Z`
