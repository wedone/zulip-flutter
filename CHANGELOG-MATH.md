# math 版更新日志

本文件记录 for-math 分支的所有重要变更。

## 版本号规则：`30.0.272-math-vX.Y+Z`

- `30.0.272`：上游 Zulip 客户端基准版本
- `math-vX.Y`：数学功能版本（X=大版本，Y=小版本）
- `+Z`：构建号（每次打包递增）

## math-v2.0（2026-06-24）

### 应用内在线升级
- 新增应用内版本检查功能，通过 GitHub Releases API 检测新版本
- 应用启动时自动检查更新（24小时内只检查一次）
- "关于"页面新增"检查更新"入口
- 检测到新版本时弹出升级提示对话框，支持查看更新说明和一键下载
- GitHub Actions 构建后自动发布到 GitHub Releases

### 版本号
- math 版本号大版本升级：v1.4 → v2.0
- 上游基准版本 30.0.272 不变

## math-v1.4（2026-06-22）

### 数学符号工具栏
- 重构常用标签布局：左栏为数字+运算符+括号+上下标混合类型，右栏补齐 26 个大写字母
- 成对符号（`$$` `||` `()` `[]` `{}`）输入后光标自动居中
- 新增快捷符号 `²` `³`（Unicode 直接输入）
- 新增 `\vec{}` 向量模板，光标定位到花括号内
- 输入框底部新增删除和光标移动按钮
- 长按删除按钮：清空全部内容
- 长按光标移动按钮：光标直接跳到最前/最后
- 大写字母长按直接输入对应小写字母

### LaTeX 实时预览
- 光标位于公式界定符内时自动渲染预览
- 支持 `$...$` `$$...$$` `\(...\)` `\[...\]`
- 300ms 防抖，失焦自动隐藏，节省渲染资源

### 工程优化
- 优化 APK 构建 workflow，简化 artifact 名称格式
- 版本号格式统一为 `30.0.272-math-vX.Y+Z`
- 清理冗余文档，集中管理版本历史

## math-v1.3（2026-06-21）

### LaTeX 实时预览
- 新增输入框内实时公式渲染预览功能
- 仅预览光标所在位置的公式，避免多余渲染
- 渲染失败时不显示预览区（与"无公式"行为一致）
- 输入框失焦后预览区域自动消失
- 300ms 防抖机制

## math-v1.2（2026-06-21）

### LaTeX 界定符自动转换
- 发送新消息时自动将通用 LaTeX 界定符转换为 Zulip 格式
- 转换规则：
  - `$...$` → `$$...$$`（行内公式）
  - `\(...\)` → `$$...$$`（行内公式）
  - `$$...$$`（含换行）→ `` ```math\n...\n``` ``（独立公式）
  - `\[...\]` → `` ```math\n...\n``` ``（独立公式）
- 保护机制：代码块、行内代码、已有 math 块、转义 `\$` 不转换
- 编辑已有消息时不进行转换
- 设置 > 实验性功能 > 自动转换 LaTeX 界定符（默认开启）

### 数学符号工具栏
- 6 个分类标签：常用、关系与集合、函数、希腊字母、公式模板、最近
- 最近使用符号持久化（最多存储 20 个，最近使用的排在最前）
- LaTeX 模板插入后光标自动定位到待填位置
- 支持选中文字后用公式包裹（`$...$`、`$$...$$`）
- 工具栏展开后保持打开，需手动关闭

## math-v1.1（2026-06-21）

### 数学公式渲染优化
- 使用 flutter_math_fork 替代官方 KaTeX HTML/CSS 解析方案
- 移除 ~1200 行 KaTeX 解析代码，降低维护成本
- 完整支持 KaTeX 语法，渲染与 Web 端一致
- 移除 21 个 KaTeX TTF 字体依赖
- 修复 Android 端 KaTeX_Math 斜体 Bug（flutter#167474）

## math-v1.0（2026-05-15）

- 基于 Zulip 30.0.272 分叉
- 初始数学功能版本
